#include<bits/stdc++.h>
using namespace std;
int solve() {
    int n, m, a, b, c;
    scanf("%d%d%d%d%d", &n, &m, &a, &b, &c);
    vector<int> e[200005];
    for (int i=0;i<m;i++)
        scanf("%d", &a[i]);
    printf("%d\n", );
    return 0;
}
int main() {
    int t;
    scanf("%d", &t);
    while (t--) {
        solve();
    }
    return 0;
}
