#include<bits/stdc++.h>
using namespace std;
int solve() {
    int n, a[1000000], b[1000000];
    scanf("%d", &n);
    for (int i=0;i<n;i++) {
        scanf("%d", &a[i]);
    }
    for (int i=0;i<n;i++) {
        scanf("%d", &b[i]);
    }
    int h;
    for (int i=0;i<n;i++) {
        if (b[i]>a[i])
    }
    return 0;
}
int main() {
    int t=1;
    scanf("%d", &t);
    while (t--) {
        solve();
    }
    return 0;
}
